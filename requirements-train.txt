# Training / Notebook dependencies (NOT used for Vercel deployment)

# Core ML stack
numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.5.0
scipy>=1.10.0
joblib>=1.3.0

# Utilities
tqdm>=4.60.0
h5py>=3.0.0

# (Optional) If you re-run the API locally using the same env:
fastapi>=0.100.0
uvicorn>=0.30.0
pydantic>=2.0.0
